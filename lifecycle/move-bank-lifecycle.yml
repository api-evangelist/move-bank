generated: '2026-07-20'
method: searched
source: >
  Consumer Data Standards (consumerdatastandardsaustralia.github.io/standards) +
  openapi/move-bank-cds-banking-products-openapi.yml + live probe (review.yml).
versioning:
  scheme: header
  mechanism: x-v (requested max) / x-min-v (requested min) with x-v response header
  current:
    get_products: 4
    get_product_detail: 4
  confirmed: >
    Get Products returns x-v 4; x-v 1/2/3/5 return 406 UnsupportedVersion
    ("Available 4"). Version is per-endpoint, not a global path/date version.
  docs: https://consumerdatastandardsaustralia.github.io/standards/
deprecation:
  policy: cds-endpoint-versioning
  mechanism: >
    The Consumer Data Standards retire endpoint versions by publishing them as
    obsolete (the harvested spec links obsolete get-accounts v1/v2 pages) and by
    advancing the supported x-v range; unsupported versions return 406. There is
    no RFC 8594 Sunset/Deprecation HTTP header on the public PRD.
  obsolete_versions_documented: true
  policy_url: https://consumerdatastandardsaustralia.github.io/standards/
status_page:
  found: false
  note: >
    No MOVE Bank-hosted API status page was found. CDR outage/availability
    reporting for data holders is coordinated at the regime level (ACCC / CDR),
    not via a bank-published status.microsite.
sla:
  found: false
  note: CDR performance thresholds are set by the DSB regime, not a MOVE Bank SLA page.
deprecated_operations: []
